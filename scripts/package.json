{
  "name": "splot-tile-generator",
  "version": "1.0.0",
  "description": "Generate protomaps pmtiles for Splot",
  "type": "module",
  "scripts": {
    "generate": "node generate-tiles.js",
    "upload": "node upload-tiles.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.716.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  },
  "notes": {
    "pmtiles-cli": "Requires pmtiles CLI tool (go-pmtiles) to be installed. Download from https://github.com/protomaps/go-pmtiles/releases"
  }
}